org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Algunos plugins (flutter_timezone, pedometer, flutter_contacts) aún declaran
# Kotlin 1.8 / Java 11, lo que dispara "Inconsistent JVM Target Compatibility".
# La mezcla es funcionalmente válida en Android; relajamos la validación a aviso.
kotlin.jvm.target.validation.mode=warning
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
